node_modules
.next
dist
pnpm-lock.yaml

# The owner's prose, not our source. Prettier reflows markdown tables and rewraps
# lists, which turns a one-line spec edit into a hundred-line diff.
CLAUDE.md
docs/
design/

# A verbatim copy of design/assets/ds.css. Reformatting it would make the two
# impossible to diff, which is the only way to tell whether they have drifted.
packages/ui/src/components.css
.claude/
spike/
README.md
packages/db/prisma/migrations/
packages/db/generated/
